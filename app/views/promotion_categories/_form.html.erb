<%= form_for(@promotion_category) do |f| %>
  <% if @promotion_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promotion_category.errors.count, "error") %> prohibited this promotion_category from being saved:</h2>

      <ul>
      <% @promotion_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :promotion_id %><br>
    <%= f.number_field :promotion_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.number_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
