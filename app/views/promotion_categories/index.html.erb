<h1>Listing promotion_categories</h1>

<table>
  <thead>
    <tr>
      <th>Promotion</th>
      <th>Category</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @promotion_categories.each do |promotion_category| %>
      <tr>
        <td><%= promotion_category.promotion_id %></td>
        <td><%= promotion_category.category_id %></td>
        <td><%= link_to 'Show', promotion_category %></td>
        <td><%= link_to 'Edit', edit_promotion_category_path(promotion_category) %></td>
        <td><%= link_to 'Destroy', promotion_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Promotion category', new_promotion_category_path %>
